[project]
name = "ai-shorts-video"
version = "0.6.0"
description = "Miko AI Shorts native Cloudflare Python Worker"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
