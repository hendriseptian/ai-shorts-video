[project]
name = "ai-shorts-video"
version = "0.2.0"
description = "Miko AI Shorts backend running on Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115,<1.0"
]

[dependency-groups]
dev = [
    "workers-py==1.16.2",
    "workers-runtime-sdk==1.6.8"
]
